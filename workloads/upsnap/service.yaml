apiVersion: v1
kind: Service
metadata:
  name: upsnap
  namespace: upsnap
spec:
  type: NodePort
  ports:
    - port: 8090
      targetPort: 8090
      nodePort: 30090
      name: http
  selector:
    app: upsnap
