apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: upsnap-data
  namespace: upsnap
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
